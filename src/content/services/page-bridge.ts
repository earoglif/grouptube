export { PAGE_BRIDGE_EVENTS } from "../../shared/page-bridge/events";

export function ensurePageBridgeInjected(): void {
  // Page script is registered as a content_script with world: "MAIN" in the manifest.
}
